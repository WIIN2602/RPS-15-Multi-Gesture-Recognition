# 🖐️ RPS-15: Advanced Hand-Gesture Recognition

โปรเจคเป่ายิ้งฉุบยุคใหม่ที่ก้าวข้ามขีดจำกัดเดิมๆ จาก 3 ท่า (ค้อน-กรรไกร-กระดาษ) สู่ **15 ท่าทางตามหลักสากล** โดยใช้พลังของ Computer Vision และ Machine Learning

### 🌟 ฟีเจอร์หลัก (Current Progress)

* **Real-time Hand Tracking:** ตรวจจับและวาดจุด Landmark บนมือ 21 จุดด้วย MediaPipe
* **Professional Architecture:** โครงสร้างโปรเจคแบบ Modular แยกส่วน Engine และ Logic ชัดเจน
* **Environment Configuration:** รองรับการตั้งค่าผ่านไฟล์ `.env` เพื่อความปลอดภัยและยืดหยุ่น
* **Virtual Environment Support:** พร้อมใช้งานผ่าน `.venv` เพื่อการจัดการ Library ที่สะอาด

---

### 📂 โครงสร้างโฟลเดอร์

```text
RPS15_Project/
├── .venv/               # สภาพแวดล้อมจำลองสำหรับ Python
├── modules/             # โมดูลหลักในการประมวลผล
│   └── hand_detector.py # ระบบสแกนและตรวจจับมือ (บทที่ 1)
├── main.py              # ไฟล์หลักสำหรับรันโปรแกรม
├── .env                 # ค่าคงที่และการตั้งค่าระบบ
└── requirements.txt     # รายการ Library ที่จำเป็น

```

---

### 🛠️ วิธีการติดตั้ง (Installation)

1. **Clone โปรเจค:**
```bash
git clone <your-repository-url>
cd RPS15_Project

```


2. **สร้างและใช้งาน Virtual Environment:**
```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# Mac/Linux
python3 -m venv .venv
source .venv/bin/activate

```


3. **ติดตั้ง Library ที่จำเป็น:**
```bash
pip install -r requirements.txt

```


4. **ตั้งค่า .env:**
สร้างไฟล์ `.env` และกำหนดค่าดังนี้:
```env
CAM_ID=0
DETECTION_CONFIDENCE=0.7
MAX_HANDS=2

```



---

### 🚀 วิธีการใช้งาน

รันโปรแกรมผ่านคำสั่ง:

```bash
python main.py

```

---

### 📈 แผนการพัฒนา (Roadmap)

* [x] **บทที่ 1:** ระบบตรวจจับ Landmark มือ (Core Engine)
* [ ] **บทที่ 2:** ตรรกะการจำแนกท่าทาง (Gesture Logic) สำหรับ 15 ท่าทาง
* [ ] **บทที่ 3:** ระบบตัดสินผลแพ้-ชนะ และ UI แสดงผลบนหน้าจอ

---

## 📝 บันทึกการอัปเดต (Changelog)

### [2026-02-19] - บทที่ 1: รากฐานของระบบ

* สร้างโครงสร้างโปรเจคแบบมืออาชีพ (Modular Structure)
* เพิ่มระบบจัดการ Environment ผ่าน `.env` และ `.venv`
* พัฒนา `HandDetector` Class เพื่อใช้สแกนจุด Landmark 21 จุดแบบ Real-time